# چک‌لیست تست موبایل — GoldCalculator 1.2

بعد از `npm install` و `npm run dev` موارد زیر را بررسی کنید:

- [ ] صفحه محاسبه بدون اسکرول اضافی و با safe-area مناسب باز شود.
- [ ] قیمت‌ها به تومان و با جداکننده هزارگان نمایش داده شوند.
- [ ] وزن اعشاری مثل `3.25` قابل ورود باشد.
- [ ] تغییر بین ۱۸ و ۲۴ عیار درست عمل کند.
- [ ] اجرت، سود و مالیات بعد از بستن/بازکردن مرورگر باقی بمانند.
- [ ] تاریخچه و نمودار بعد از Refresh باقی بمانند.
- [ ] تم روشن، تیره و خودکار درست نمایش داده شوند.
- [ ] در حالت آفلاین بنر «آفلاین» نمایش داده شود.
- [ ] در حالت آفلاین محاسبه و تاریخچه همچنان کار کنند.
- [ ] در حالت آفلاین دکمه API پیام قابل فهم نمایش دهد.
- [ ] در مرورگر پشتیبانی‌شده گزینه «نصب روی موبایل» داخل منوی قابلیت‌ها ظاهر شود.
- [ ] بعد از نصب، برنامه بدون نوار آدرس مرورگر باز شود.
- [ ] آیکون برنامه روی Home Screen واضح و صحیح باشد.

## نکته نصب

Install Prompt معمولاً فقط روی HTTPS یا localhost فعال می‌شود. برای تست نصب روی گوشی از یک آدرس HTTPS استفاده کنید.
